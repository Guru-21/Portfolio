import Typed from 'typed.js';

var options = {
  strings: ["Nice to meet you!", "I'm a Java Backend Developer", 'I have 3+ years of experience in the IT Industry.'],
  typeSpeed: 120,
  backSpeed: 50,
  backDelay: 1500,
  loop: true
};

var typed = new Typed('#headline', options);
